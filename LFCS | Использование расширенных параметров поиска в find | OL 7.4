mkdir  -p  find/hostfiles
find /etc -name "*host*"
find /etc -name "*host*" 2>/dev/null 
find /etc -name "*host*" -exec  cp {} find/hostfile \;
cd find/hostfile
ll host/
ll host/ | grep host 

mkdir find/sizefiles 
man find 
/size 
find /etc -size +1M
find /etc -size +1M -exec cp {} find/sizefiles \;
cd find/size
ls
ll -h 

mkdir  find\user
grep  idenis /etc/* 
grep  idenis /etc/* 2>/dev/null
grep -l  idenis /etc/* 2>/dev/null
find /etc/ -exec grep -l idenis  {} \; 2>/dev/null 
find  /etc -exec  grep -l user {} \; -exec cp {} find\user \; 2>/dev/null
cd find\user
ll


-exec  cp {} find/hostfile \; - копирование в директорию hostfile
-size +1M - файлы больше 1 Megabytes
grep -l - вывод только файлов 
